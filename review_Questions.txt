1. C++程序的模块叫什么？
    函数

2. #include <iostream> 有什么用
    在最终的编译之前，使用iostream文件的内容替换该编译指令。

3. using namespace std 有什么用
    程序可以使用std名称空间中的定义

4. 什么语句打印“Hello, world”, 然后开始新的一行
   cout << "Hello, world" << endl;

5. 什么语句创建名为cheeses的整数变量？
    int cheeses;

6. 什么语句将值32赋给变量cheeses？
    cheeses = 32;

7. 什么语句将键盘输入的值读入变量cheeses中？
    cin >> cheeses;

8. 什么语句打印“We have X varieties of cheese”, 用cheeses变量的当前值来代替X
    cout << "We have "<< cheeses << " varieties of cheese" << endl;
9. 下面的函数原型指出了关于函数的那些信息？
    int froop(double t); 返回值是int型， 参数是double
    void rattle(int n);  返回值为空， 参数是int
    int  prune(void);    返回值int，参数为空
10. 定义函数时，在什么情况下不必使用关键字return
    void func(...)